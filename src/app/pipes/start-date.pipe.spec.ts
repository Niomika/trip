import { StartDatePipe } from './start-date.pipe';

describe('StartDatePipe', () => {
  it('create an instance', () => {
    const pipe = new StartDatePipe();
    expect(pipe).toBeTruthy();
  });
});
