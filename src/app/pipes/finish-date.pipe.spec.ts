import { FinishDatePipe } from './finish-date.pipe';

describe('FinishDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FinishDatePipe();
    expect(pipe).toBeTruthy();
  });
});
